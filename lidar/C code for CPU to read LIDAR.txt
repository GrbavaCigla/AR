//kako cpu cita LIDAR
#define LIDAR_STATUS   (*(volatile unsigned int*)0x80000000)
#define LIDAR_ANGLE    (*(volatile unsigned int*)0x80000004)
#define LIDAR_DISTANCE (*(volatile unsigned int*)0x80000008)
#define LIDAR_CONTROL  (*(volatile unsigned int*)0x8000000C)

while(1)
{
    if(LIDAR_STATUS & 1)
    {
        int angle = LIDAR_ANGLE;
        int dist  = LIDAR_DISTANCE;

        LIDAR_CONTROL = 1;

        // obrada
    }
}